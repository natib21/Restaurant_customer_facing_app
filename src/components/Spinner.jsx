import React from 'react';
// import './Spinner.css'; // We'll define the CSS next

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;